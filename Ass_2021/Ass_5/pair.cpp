#include<iostream>
#include<vector>
#include<stack>
#include<queue>
using namespace std;
int main(){
    int a,b;
    cin>>a>>b;
    vector<vector<pair<int,int>>>vvp;
    pair<int,int>p;
    p = make_pair(a,b);
    vector<pair<int,int>>vp;

    for(auto ele: vp){
        ele.first;
        ele.second;
    }
    // p.first = a;
    // p.second = b;

    cout<<p.first<<" "<<p.second;
}